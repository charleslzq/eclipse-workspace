/**
 * 
 */
package src.interfaces;

/**
 * This interface is used to sort classes that based on rectangles.
 * @author zqliu
 * @version 1.0
 */
public interface Sorter {
	
	/**
	 * This method are supposed to compare 
	 * two arguments, each of which is supposed to 
	 * contain a rectangle to specify its position.
	 * If the first one, compared to the second one,
	 * is higher or lefter with the same Y+height, 
	 * then the result will be true;
	 * @return The result of determining whether a is higher or lefter with the same Y+height than b or not.
	 */
	public boolean isHigherLefter(Sorter a, Sorter b);
}
